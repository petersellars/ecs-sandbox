[
  {
    "name": "${name}",
    "image": "${image}",
    "cpu" : 1024,
    "memory": 2000,
    "entryPoint": [],
    "environment": [],
    "command": [],
    "portMappings": [
      {
        "hostPort": ${port},
        "containerPort": ${port},
        "protocol": "tcp"
      }
    ],
    "volumesFrom": [],
    "links": [],
    "mountPoints": [],
    "essential": true,
    "logConfiguration": {
      "logDriver": "awslogs",
      "options": {
        "awslogs-group": "awslogs-${name}",
        "awslogs-region": "ap-southeast-2",
        "awslogs-stream-prefix": "${environment}"
      }
    }
  }
]
